# Optional: Discord User Token (NOT Bot Token)
# If not provided, the program will prompt for authentication
DISCORD_TOKEN=

# Optional: Default delay between deletions (in milliseconds)
DELETE_DELAY=1000